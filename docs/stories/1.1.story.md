# Story 1.1: Search Input

**Status:** Done

**Story**
As a learner, I want a single, clear input field on the main screen where I can type or paste Japanese text (a kanji, a word, or a sentence), a reading (kana), or select a component (radical/primitive) to begin my search.

**Acceptance Criteria (ACs)**

1. The main screen has a single input field.
2. The input field supports text input, paste, and component selection.
3. The input field is clear and easy to use.
4. The system correctly identifies and extracts the input type (kanji, word, sentence, kana, radical, primitive).

**Dev Technical Guidance**

* **Previous Story Insights**: No previous story.
* **Data Models**: No specific data models are required for this story, as it focuses on the UI input component. The data will be handled in later stories. [Source: architecture/data-models.md]
* **API Specifications**: No API interaction is required for this story. [Source: architecture/rest-api-spec.md]
* **Component Specifications**:
  * A new component, `kanji-search`, should be created.
  * This component will contain the search input field.
  * The input field should be implemented using Angular Material's `matInput` for a consistent look and feel. [Source: architecture/tech-stack.md]
* **File Locations**:
  * Create the new component at `apps/jp-aid/src/app/components/kanji-search/`. [Source: architecture/application-architecture.md]
* **Testing Requirements**:
  * Unit tests should be created for the `kanji-search` component.
  * Tests should verify that the input field is rendered correctly.
  * Tests should cover the different input types (text, paste, component selection). [Source: architecture/testing-strategy.md]
* **Technical Constraints**:
  * The component should be built using Angular and TypeScript. [Source: architecture/tech-stack.md]

**Tasks / Subtasks**

1. [x] **(AC: 1, 2, 3)** Create a new Angular component named `kanji-search` at `apps/jp-aid/src/app/components/kanji-search/`.
    * [x] Implement a single input field using Angular Material's `matInput`.
    * [x] Ensure the input field is styled according to the application's theme.
2. [x] **(AC: 4)** Implement basic logic to handle text input and paste events.
    * [x] For now, the logic can simply store the input value in a component property.
    * [ ] The component selection feature will be implemented in a future story.
3. [x] **(AC: 1, 2, 3)** Add the `kanji-search` component to the main application view.
4. [x] Create unit tests for the `kanji-search` component.
    * [x] Write a test to ensure the component renders the input field.
    * [x] Write tests to simulate user input and verify the component's state.

**Project Structure Notes**
The `apps/jp-aid/src/app/kanji-search` directory does not exist and may need to be created.

**Dev Agent Record**

* **Completion Notes**: Centering layout for the component will be done in another story.
* **Debug Log**: None
* **Change Log**: None
* **File List**:
  * `apps/jp-aid/src/app/kanji-search/kanji-search.ts`
  * `apps/jp-aid/src/app/kanji-search/kanji-search.html`
  * `apps/jp-aid/src/app/kanji-search/kanji-search.scss`
  * `apps/jp-aid/src/app/kanji-search/kanji-search.spec.ts`
  * `apps/jp-aid/src/app/app.ts`
  * `apps/jp-aid/src/app/app.html`
  * `apps/jp-aid/src/app/app.spec.ts`
